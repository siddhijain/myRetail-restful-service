package com.target.model;

import org.springframework.data.mongodb.repository.MongoRepository;

public interface ProductPriceRepository extends MongoRepository<ProductPriceDetails,Long>{
	
	public ProductPriceDetails findById(Long id);

}
